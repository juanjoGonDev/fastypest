{
  "testEnvironment": "node",
  "preset": "ts-jest",
  "rootDir": "tests",
  "setupFilesAfterEnv": ["./config/jest.setup.ts"],
  "testRegex": ".spec.ts",
  "transform": {
    "^.+\\.tsx?$": [
      "ts-jest",
      {
        "tsconfig": "tsconfig.json"
      }
    ]
  }
}
